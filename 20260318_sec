num = 3 # if 예시
if num > 0 :
    print(num, "is a positive number")
print("This is always printed.")

num = -1
if num > 0 :
    print(num, "is a positive number")
print("This is also always printed")

num = 3 # if else 예시
if num >= 0 :
    print("Positive or Zero")
else :
    print("Negative number")

num = 3.4 # if elif else 예시

if num > 0 :
    print("Positive or Zero")
elif num == 0 :
    print("Zero")
else :
    print("Negative number") 

# List of numbers / for loop 예제
numbers = [6, 5, 3, 8, 4, 2, 5, 4, 11]

# variable to store the sum
sum = 0

# iterate over the list
for val in numbers :
    sum = sum + val
print("the sum is", sum)

genre = ['pop', 'rock', 'jazz'] # for lop + rnage() 예시1
#in, len,__getitem__ 연산자 지원함
#iterate over the list using index
for i in range(len(genre)):
    print("I like", genre[i])

genre = [0, 1, 2] # for lop + rnage() 예시1-2
#in, len,__getitem__ 연산자 지원함
#iterate over the list using index
for i in range(len(genre)):
    print("I like", genre[i])

print(range(10)) # for loop + range() 예시2

print(list(range(10)))

print(list(range(2, 8)))

print(list(range(2, 20, 3)))

n = int(input("Enter n : ")) # While loop 예시

#initialize sum and counter
sum = 0
i = 1

while i <= n :
    sum = sum + i 
    i = i +1   # update counter

# print the sum
print("The sum is", sum)

counter = 0 # While loop + else 예시, 많이 안 씀

while counter < 3 :
    print("Inside loop")
    counter = counter + 1
else :
    print("Inside else")

for val in "string" : # 강제적으로 멈추거나 건너뛰고 싶은 반복문 아래에 둬야 됨
    if val == "i" :
        break
        # continue
    print(val)
print("The end")




    
